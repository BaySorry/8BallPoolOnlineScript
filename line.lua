--long line game:GetService("Workspace").Tables.Table1.Guides.HitTrajectory.Size = Vector3.new(0.05, 0.05, 25) --move opponent's ball PATCHED --remove opponent's 8 ball for i,v in pairs(getnilinstances())do if v.Name=="EventHandlerClient"then ev=v end end game.RunService.Stepped:Connect(function() require(ev):FireRemoteEvent("UpdateBalls",{["8Ball"] = CFrame.new(1, 1, 1)}) end) --remove all opponent's ball for i,v in pairs(getnilinstances()) do if v.Name=="EventHandlerClient"then ev=v end end while wait(0.2) do for i = 1, 15 do require(ev):FireRemoteEvent("UpdateBalls",{[i.."Ball"] = CFrame.new(1, 1, 1)}) end end
